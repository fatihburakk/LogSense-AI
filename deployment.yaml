apiVersion: v1
kind: Namespace
metadata:
  name: logsense
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: logsense-config
  namespace: logsense
data:
  ANOMALY_THRESHOLD: "0.5"
  LLM_TIMEOUT_SECONDS: "12"
  BUFFER_SIZE: "100"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: logsense
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: backend
        image: ghcr.io/fatihburakk/logsense-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        resources:
          limits:
            memory: "600Mi"
            cpu: "500m"
          requests:
            memory: "300Mi"
            cpu: "100m"
        envFrom:
        - configMapRef:
            name: logsense-config
        env:
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: logsense-secrets
              key: OPENAI_API_KEY
        - name: LOGSENSE_API_KEY
          valueFrom:
            secretKeyRef:
              name: logsense-secrets
              key: LOGSENSE_API_KEY
        livenessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: logsense
spec:
  type: NodePort
  selector:
    app: backend
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    nodePort: 30000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: logsense
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: frontend
        image: ghcr.io/fatihburakk/logsense-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        resources:
          limits:
            memory: "400Mi"
            cpu: "400m"
          requests:
            memory: "200Mi"
            cpu: "100m"
        env:
        - name: NEXT_PUBLIC_API_URL
          value: "http://<SUNUCU_IP>:30000"
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 60
          periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: logsense
spec:
  type: NodePort # LoadBalancer yerine NodePort'a geçtik
  selector:
    app: frontend
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    nodePort: 30001 # Frontend artık bu porttan açılacak
